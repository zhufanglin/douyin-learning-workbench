# 验证记录

日期：2026-09-09。范围：本地非商业学习原型，单用户单进程。

## 最新完成：四个数据入口与任务层级浏览

- 四张统计卡和侧栏共用累计任务、已存视频、去重用户、已存评论入口；取消原来的用户/视频/评论并列标签。任务按执行时间排序，视频按任务分组，用户及评论按视频分组。
- 新增只读 catalog 接口，覆盖全部非模拟历史任务；按任务、关键词及日期筛选时读取范围内任务快照。测试覆盖超过 100 个任务、跨来源相同编号、历史正文不被较新记录替代、日期午夜边界及非法日期。
- 视频单独进入评论详情，优先显示当次任务记录，其他独立读取结果明确标注来源。浏览已有记录不发起平台写请求；没有记录时提供明确读取按钮，目录读取失败时先核对。保留暂停、评论/回复续读、100 条分页与导出。
- 评论和回复作者可打开用户侧栏，按当前视频或全部相关视频查看最新已存评论；关闭侧栏保持原评论页。返回目录保留筛选和页码，刷新恢复所选任务及视频。
- 针对性整组 **19 passed，1 warning，40.08 秒**；最终关联选择规则调整后再次通过 7 项 UI 回归。前端生产构建成功，当前服务和内置浏览器已显示新版。
- 实际只读核验目录：60 个任务、587 个去重视频、1,220 位用户、1,281 条评论，模拟任务为 0。所有本轮界面操作测试使用临时数据库/本地夹具，不额外访问抖音。截图在 `docs/evidence/library/`，已检查桌面和 390px 窄屏。
- 限制：用户侧栏展示最新汇总；只有任务详情和任务筛选保证对应历史快照。未记录父任务的独立读取不猜测搜索归属。目录在本地构建，本轮未验证大规模数据压力。

## 最新完成：移除日常界面的模拟功能

- 删除模拟搜索来源选择、模拟关注/私信、消息输入、模拟导航与记录标签；新建搜索显式提交 `source=live`，用户没有点击搜索前不会访问平台。统计卡改为任务、视频、用户和评论。
- 页面请求 `state?include_demo=false`，SQL 在条数限制前筛选网页读取与本地导入，统一过滤模拟任务、用户、日志、操作及统计。原数据库未删除记录，默认内部 API 与测试样例继续可用于回归。历史模拟任务选中缓存会回退到可见任务，避免刷新后再次显示模拟内容。
- 针对 105 条内部任务覆盖真实任务的边界新增回归，先复现过滤失效，再验证可见数据完整且历史样例仍保留。任务与界面测试共 **18 passed，1 warning，34.99 秒**。
- 界面搜索测试拦截全部请求，验证明确使用 live、旧缓存回退、导出、导入、刷新及模拟按钮缺席；评论/回复分页测试仍使用内部夹具，没有额外访问抖音。
- 最终前端构建成功；服务已重启。实际状态接口核验模拟任务、用户和操作均为 0；界面截图及 390/768/1280/1440 宽度、主题和导航检查已更新。

## 最新完成：沿用旧版电商工作台视觉

- 参考用户电商项目改造前的 `audit-evidence/final-review/02-app-dashboard.png` 和 `1688-agent-workbench/brand-spec.md`：白色侧栏、浅蓝灰底、钴蓝主色、圆角卡片。未采用 RunningHub 页面；电商项目只读。
- 改造当前工作台外壳、搜索区域、统计卡、视频/评论区域与日志样式；新增锚点导航，保留既有查询、分页、导入导出和操作门禁。统计数据仍来自本地 API，等待连接显示破折号。
- UI 回归：`tests_learning/test_ui.py` **7 passed，43.09 秒**，覆盖模拟搜索/去重操作/导出/导入/刷新、主评论分页、回复及图片状态。随后修正新增导航的输入框焦点，并完成独立导航检查；没有修改后端。
- 最终生产构建成功；独立页面检查在 390 / 768 / 1280 / 1440 宽度均无整页横向溢出，导航不触发写请求，主题切换通过，无页面脚本异常。已查看桌面、手机和深色截图。
- `docs/evidence/ui-refresh/` 为独立模拟夹具截图，不代表新增真实抓取成绩；`visual-check.json` 保存检查结果。本次未额外执行抖音访问或账号操作。

## 最新完成：真实图片标识与完整流程

- 对视频 `7669260461917816817` 做普通页面滚动和截图，确认评论者附带的猫照片位于正文外的 `.RDy3JwM0 > img.duf1cdUE`，并非头像或右侧推荐图。此前小图定位截图超时；整页截图配合普通滚动取得证据，没有绕过验证或请求图片接口。
- 图片适配增加这一已观察结构，范围仍严格限定在对应 comment-item 内，保持头像/表情等排除规则。新增对应主评论和回复夹具，先复现漏读，再验证图片去重、100 / 5 回复分页、元数据保存与未读取状态。读取器版本升至 4，允许旧版未支持终态人工补读新版标识。
- 通过真实本地界面重新选择该视频，任务 `e25da94ed17b48328bd68b513553f2d4` 首批保存 100 条主评论（96 条含图片），点击下一批后为 200 条（195 条含图片）；200 个唯一用户、200 个唯一评论 ID，revision=1、exhausted=false。图片记录全部有作者和路径摘要，其中 3 条为纯图片占位。
- 真实工作台已核验“图片内容未读取（1 张）”和纯图片占位。直接下载 JSON 与数据库结果一致；上一页/下一页和刷新恢复均未改变 revision、评论数或保存内容。成功状态只表示本批记录已保存，任务说明明确提示 195 条图片内容未读取，并未宣称识别图片内容或读取全部评论。
- 本轮核心流程完成：严格视频筛选的“晒猫”任务仍为 67 条部分结果，未达 100 条；选中视频的评论 100 → 200、用户去重、图片存在标识、刷新和导出验证通过。真实图片主评论已验证；图片回复的完整现场读取尚未单独验证，普通文字/表情回复仍有前述真实验证记录。
- 最终整组 **62 passed，0 skipped，1 warning，72.81 秒**（54 项后端/本地页面、7 项 UI、1 项启动脚本）。最终前端构建通过，已运行服务提供最新图片提示与升级入口。

## 最新补充：换关键词完整流程复测

- 使用本地真实界面输入“晒猫”并启动网页读取。初次任务 `52f24afbc0fd445e814c85318c618ac9` 得到 100 个不同编号，但混入明确标注“图文”的帖子；选择图文编号 `7609192429061352890` 后地址发生类型切换，评论任务 `590fe936db534a468d64129ab001f977` 保留 needs_review 和 0 评论，没有把其他页面数据混入。
- 首次仅排除“图文”标签后，任务 `9d4198d54d6f4a3eb48a4f95b33e02e5` 又暴露相关搜索卡片和 `1 / 1` 图片卡片混入。最终规则改为：搜索卡片存在时，只接受第一行显示视频时长的卡片，不混用页面全局链接，按卡片出现顺序保存。新增回归覆盖上述三种非视频卡片以及“图文制作教程”这类正常视频标题，避免按标题关键词误删。
- 上述两次混合搜索任务已明确改为 needs_review 并补充原因日志，原始结果保留。旧版其他 100 条搜索结果仅是历史数量验证，没有补做严格内容类型核验，不将其宣称为新版纯视频结果。
- 最终搜索任务 `f2f5e8a7acb84764b49fe68580a34f5f` 返回 67 条视频，status=partial，提示页面暂未提供更多可识别结果。保存的视频均带 `content_type=video` 和页面时长 `duration`，编号唯一，顺序连续；未凑满 100 条，也不声称平台只有 67 条。
- 通过本地界面选择第 1 条视频 `7669260461917816817`，评论任务 `a2f36fcb9fad4715aa787739729724f3` 首批保存 100 条，点击“下一批 100 条”后累计 200 条，revision=1、status=success、exhausted=false。200 个评论 ID 与 200 个用户 ID 均唯一，尚未声明主评论全部读完。
- 实际点击上一页/下一页、下载本视频 JSON、刷新后恢复所选视频：导出与保存记录逐条一致，刷新与浏览旧页未增加 revision 或评论记录，UI 无脚本错误。这些是实际 live 任务，不是 HTTP 夹具。此时旧图片规则未识别正文外附件；后续修复与真实图片验证见上方最新记录，旧任务快照保留原样。
- 搜索规则变更后整组 **60 passed，0 skipped，1 warning，78.60 秒**。本次界面只把固定“100 条已验证”改为“已验证 · 最多 100 条”，另行构建验证。没有执行任何真实关注或消息。

## 最新补充：图片评论标识

- 主评论和回复共用图片标识读取：正文内或带明确评论图片标记的图片保留作者、数量与路径摘要；纯图片正文为“图片内容未读取”，图文混合保留原文字并加未读取提示。不获取图片字节，不把占位提示当成图片全文。头像、链接、按钮、隐藏图、已知表情节点不作为评论图片。
- 本地 Chromium 夹具验证：同一作者的不同图片分别保存，只有地址查询参数变化时不重复保存；图文混合沿用旧文字 ID，允许补充图片标识；正文外图片必须有明确评论图片标记。无可用图片地址且没有文字的未知行保留未支持，不按行号编造稳定 ID。
- 新增图片回复 105 条夹具，分别按 100 / 5 条续读、SQLite 保存和恢复；页尾状态保持 unsupported，按钮禁止重复，主评论页尾状态独立。图片占位数量与未识别行数量分开记录。主评论有图片标识时，即使确认主评论页尾也保留 partial 和图片未读取说明。
- 界面夹具覆盖版本 1/2 补读入口、版本 3 终态禁用、主评论图文提示、回复图片占位和 JSON 下载；导出保留 `content_status=image_not_read` 与 `image_count`，不渲染或请求原图片。
- 最新整组运行 **59 passed，0 skipped，1 warning，59.27 秒**：52 项后端/本地页面测试、6 项 UI 测试、1 项启动脚本测试。TypeScript/Vite 构建通过。以上图片读取正例均为虚构 DOM；尚未验证真实抖音图片评论的识别成功，不能据此承诺所有图片结构可用。
- 原真实视频讨论做只读回归，复用用户已授权的普通页面展开流程；148 条回复仍识别为原来的文字/表情，图片标识数为 0，临时数据未写回旧任务。没有自动关注或发送消息。
- 现场还发现两个正文外、无明确评论图片标记的 `img.duf1cdUE` 节点，未直接纳入图片规则。进一步截图核对的独立检查返回浏览器 Error，未确认节点用途；未据此扩展选择器或伪称图片实测成功。服务已恢复，历史任务未改写。
- 额外回归：主评论有图片但无可用于去重的地址时，记录 `unidentified_image_count`，到页尾保留 partial，并提示图片标识无法保存；不会静默跳过后报内容完整。

## 最新补充：纯表情回复与旧任务补读

- 新增纯表情回复解析：只读取正文内可见表情图片的方括号名称，保存类型和名称列表，不下载图片。隐藏图片、无可读名称图片不作为表情文字。沿用历史文字记录 ID；纯表情与恰好相同的字面文本分别去重。
- 本地 Chromium 夹具先复现纯表情丢失、旧版未支持终态不能补读；补齐实现后验证原记录 ID 保留、重复读取不新增、相同作者的字面文字与表情分开、作者和所属讨论关系、SQLite 保存、隐藏图片排除。旧版终态只在用户请求时升级读取，版本与 revision 共同防止重复请求。新增界面覆盖旧任务补读入口和表情显示。
- 在实际工作台点击任务 `be5e9b6752814a8d975779a3d06d2df6` 的“补读表情回复”：revision=6，status=success，主评论仍为 100 条，所选讨论回复从 146 增至 148。新增两条为 `[看]`、`[流泪]`，均关联原主评论 `dom-233e5be7806c10f0174e7b47`，作者及主页已核对存在。导出共 248 个唯一评论 ID、226 个唯一用户 ID；此前 246 条评论记录逐条比对完全未变。
- 当前回复状态：`end_state=complete`、`page_end=true`、`exhausted=true`、`unsupported_count=0`、`observed_count=148`、`reader_version=2`。真实 UI 显示“该评论回复已读完”并禁用按钮，回复第二页显示新增表情，分页为 100 / 48。再次提交当前 revision 的读取请求返回 HTTP 409，导出内容与版本完全未变。
- 最新整组运行 **53 passed，0 skipped，1 warning，46.13 秒**：47 项后端/本地页面测试、5 项 UI 测试、1 项启动脚本测试。TypeScript/Vite 构建通过。实际读取在整组测试完成之后执行，没有被启动脚本测试中断。
- 本次只补齐纯表情回复。图片评论标识、主评论纯表情及混合图文完整还原尚未实现，不将它们宣称为已支持。其余任务及分工见 `REMAINING_TASKS.md`。历史验证记录保留如下，旧计数不代表当前状态。

## 最新补充：回复结束状态

- 对任务 `be5e9b6752814a8d975779a3d06d2df6` 的同一讨论做普通页面检查：当前展开区域有 148 条回复行，其中 146 条包含可保存文字，另外 2 条仅有内嵌表情图片；未发现缺失作者的行。页面只剩“收起”，没有继续展开入口或加载提示。旧逻辑要求空正文行也存在已存 ID，因此无法确认结束。这些数量来自本次 DOM，不是平台总数承诺。
- 新逻辑区分 `complete`、`unsupported`、`blocked`，等待结构稳定后才判断结束；评论正文里的“加载中”不充当加载提示。无法保存的纯表情、图片或未识别行单独计数，不伪造文字记录。早期超时或验证中断也会保留可核对的回复状态。
- 真实复测到 revision=5：保存 146 条文字回复，`end_state=unsupported`、`page_end=true`、`unsupported_count=2`、`observed_count=148`、`exhausted=false`，任务保持 partial。界面实查显示“已到页尾 · 有未支持内容”，继续按钮禁用；已有回复仍可按 100 / 46 两页浏览。
- 对该结束状态再次提交续读，实际返回 HTTP 409“已到回复页尾，剩余内容未支持；不能重复读取同一批次。”复核导出 revision 仍为 5、共 246 个唯一评论 ID（100 主评论 + 146 回复），结束元数据保留，没有启动重复批次。
- 新增 6 项后端/页面用例及 1 项界面用例，覆盖完整结束、未支持内容、加载停滞、终态拒绝重复请求与中断状态。原界面测试修正了新任务创建后读取旧任务 ID 的时序问题。最新 TypeScript/Vite 构建通过，整组 50 项测试通过。首次只读诊断在等待页面加载时超时，后续普通页面就绪检查取得上述证据；没有绕过验证，也未将超时归因于内存。

## 最新补充：主评论到底与回复分页

- 用户再次批准继续后，原任务 `41879df3c4844f569264b4169ced1f7a` 从 revision=10 的失败状态继续至 revision=11。实际新增 1 条主评论，累计 534 条主评论记录、3 条已存回复、514 位去重用户；status=success、exhausted=true，日志为“页面明确提示主评论已到底”。这验证了新增的“暂时没有更多评论”结束文案规则在真实页面生效。
- 上次诊断截图可见正常评论，文字和结构诊断均成功。本次继续成功，未再次出现该超时；原日志未保留具体超时调用点，因此不将原因归咎于硬盘容量或确定归因于 RAM，也不声称已消除所有网页超时。
- 对同一视频再做只读检查，滚动至真实页尾；170 条可识别讨论的回复数量提示中，最大值 67，未找到大于 100 条的测试对象。该检查没有新增评论任务或操作账号。
- 新增本地真实 Chromium 夹具：205 条回复以带重叠数据的页面批次展开，应用按 100 / 100 / 5 条保存。每批重新打开 SQLite 验证继续，回复编号和顺序唯一，主评论到底标记保留；未点击另一个讨论的展开按钮或普通发送回复按钮。
- 扩展界面测试：100 / 100 / 5 条回复分三页显示，回复上一页不发采集请求，主评论页码与数量保持不变，最终导出含 205 条回复及正确的所属讨论字段。这些是虚构夹具，不等同于第三方网页实测。
- 在已有“男友穿搭”评论任务 `be5e9b6752814a8d975779a3d06d2df6`、视频 `7476890685853928739` 找到主评论 `dom-233e5be7806c10f0174e7b47`，其折叠按钮提示“展开143条回复”。普通点击后按钮变成“展开更多”，旧规则漏识别，保留 3 条回复并停为 partial。检查真实 DOM 确认为按钮内的 span；将测试夹具改成该文案后先复现失败，再补上精确匹配，5 项回复测试通过。按钮仍限定在所选讨论内，排除用户链接和评论正文。
- 修复后通过本地服务的回复接口，revision=3 首批累计 100 条回复、status=success、exhausted=false；随后在实际工作台点击“继续读取回复（最多 100 条）”，revision=4 新增 46 条，累计 146 条。界面已实际核验回复第 2 页显示 46 位作者、返回第 1 页显示 100 位作者。主评论仍为原来的 100 条，没有混入回复。
- 当时第二批状态为 partial，reply exhausted=false，未确认所有回复读完。初始折叠提示 143 与最终 146 条文字记录不能直接等同，不以数量提示宣称完整采集。此轮验证真实回复跨越 100 条的分页与继续读取；后续结束状态验证见上方最新记录。
- 实际任务导出包含 100 条主评论、146 个唯一回复 ID、225 个唯一用户 ID，所有回复均属于所选主评论；翻看已存两页不会启动新批次。所有真实数据和诊断仍只保存在 learning_data，交付包不包含登录状态和采集数据。

## 本次回复功能

- 真实任务仍为 `41879df3c4844f569264b4169ced1f7a`。主评论 `dom-a60892566b41371a09475cf1` 所在讨论显示“展开3条回复”，经普通点击后保存 3 条回复，分别带 `kind=reply`、`parent_comment_id` 和 `reply_rank`。该讨论无更多展开入口、显示收起，读取状态 success。
- 现场发现主评论与回复共用 `comment-item` 标记，回复位于 `replyContainer` 内。已据此调整主评论排除规则和讨论范围，并加入同结构的虚构回归页面，防止重复统计或混入其他讨论。
- 连续主评论分页实测经过 300、400、500，随后保存 533 条旧任务累计主评论记录及 3 条所选讨论回复。截图可见“暂时没有更多评论”；原结束文案表未覆盖该句，先保留 partial。补规则后首轮复测超时，revision=10、status=failed、exhausted=false，原数据保留；后续 revision=11 的成功验证见上方最新记录。
- 回复初期受页面加载、定位超时及结构适配影响，未成功的尝试均保留失败状态或日志。当时系统报告总 RAM 约 8 GB、空闲物理内存约 350–440 MB；这是运行内存，不能与 C 盘剩余容量混同，也不能据此断言所有超时均由内存导致。
- 本地界面测试已验证主评论下的回复分组、独立页码、读完按钮及直接 JSON 下载；导出保留所属讨论关系。这些 UI 数据为明确标记的虚构夹具，真实记录保存在 learning_data，不进入交付包。

## 本次评论分页实测

- 任务 `41879df3c4844f569264b4169ced1f7a`，视频 `7658688206654074225`。从用户已有“广州租房”结果在工作台点击第 1 条，首批成功保存 100 条主评论、90 位去重用户。
- 点击“下一批 100 条”后新增 64 条，旧版逐节点读取耗时导致暂停，状态为 partial，exhausted=false，保留 164 条。随后将 DOM 读取改为一次批量读取，普通滚动不变。
- 重启服务并在历史任务中恢复这 164 条；上一页/下一页正常。通过工作台点击继续本批，2026-09-09 16:44:52 累计达到 200 条，181 位用户，comment ID 唯一数量 200，revision=2，target=200。页面实际显示“第 2 页 · 本页 100 条”，下一批按钮可用。
- 此处为早先两页验证记录；后续累计数量和所选讨论回复见上方更新。未触发任何真实关注或私信。
- 本地自动化夹具验证 100 → 100 → 5 三页、分页去重、断点继续、旧页无新增采集请求、刷新恢复，以及评论正文内“没有更多了”不会冒充结束提示。夹具不代表真实平台完整采集已通过。

## 文档声称支持

- MediaCrawler 上游 README 列出抖音关键词搜索、评论采集、数据保存和 WebUI。
- 上游 LICENSE 为 NON-COMMERCIAL LEARNING LICENSE 1.1。
- 上述文档不能证明当前机器上的真实抖音采集可用，也没有证明关注和私信可用。

## 已实际验证

| 检查 | 结果 |
|---|---|
| Python 3.12 虚拟环境、Node.js 24、Git | 可用 |
| 原版 `npm ci` + `npm run build` | 通过 |
| 原版 API health、data/files、首页路由 | 通过；未启动原采集器 |
| 学习版后端及本地 Chromium 页面测试 | 54 项通过，含搜索类型过滤、评论批次、205 条回复续读、回复归属、去重、恢复、过期批次拒绝、结束分类、中断状态、纯表情、图片标识和旧任务升级 |
| 学习版 React/TypeScript 构建 | 通过 |
| 本地界面端到端测试 | 7 项通过，覆盖原有流程、主评论和回复各 100 / 100 / 5 分页、上一页无网络采集、刷新恢复、回复分组、直接下载、结束状态、图片标识及旧任务补读入口；数据为模拟与 HTTP 夹具 |
| 模拟“露营”搜索 | 2 视频 / 3 评论 / 2 用户 |
| 并发重复关注、同文消息去重 | 通过；均为本地模拟 |
| 导入同一用户的多条评论、来源隔离 | 通过 |
| 无效导入整批拒绝 | 通过，未留下半批数据 |
| 真实关注／私信接口调用 | 返回 409 待接入；动作记录未增加 |
| 页面导出、导入、刷新持久化 | 通过 |
| 390px 移动宽度 | 无水平溢出 |
| 停止后重新启动、数据保留 | 通过 |
| Windows 启动进程与服务进程 PID | 已修复并核验实际服务 PID 与记录一致 |
| 换端口启动 | 回归测试通过，复用同一项目的已有实例 |
| 后续导入同一 ID 的新内容 | 回归测试通过，旧任务导出保留原始快照 |
| 停止服务清理自有浏览器进程 | 实测通过，只处理核验过的本服务及其后代进程 |
| 指定真实视频读取 | 视频 7594819804352040192：1 视频 / 14 评论 / 14 用户，来源 live |
| 同一真实页面再次读取 | 两次用户和评论 ID 集合一致，数据库仍为 14 用户 / 14 评论 |
| 真实数据导出 | 实测 JSON 导出含 14 评论 / 14 用户 |
| 真实账号动作 | 数据库 live 动作为 0 |
| 真实“露营”搜索 | 100 个视频 / 100 个不同编号 / 搜索顺序 1–100 / 搜索阶段 0 评论 |
| 点击列表中的模拟视频 | 只显示所选视频的 2 条评论和 2 位用户；刷新保留原搜索任务 |
| 点击列表中的真实视频 | 在工作台点击第 1 条，成功展示 4 条真实评论、4 位用户和主页链接 |

最新整组运行结果：**62 passed，0 skipped，1 warning**（54 项后端/页面测试 + 7 项界面端到端测试 + 1 项启动脚本测试），耗时 72.81 秒，运行时真实任务已停止。本轮增加搜索类型过滤和真实正文外图片适配，前端构建通过。真实主评论图片标识正例已验证，图片回复现场流程仍未单独验证。

主评论到底任务累计 537 条评论记录（534 主评论 + 3 回复）、514 位用户；大回复讨论任务的独立导出结果见上方最新记录。旧数据保持不变，失败和部分结果尝试的日志保留。

端到端测试产生的示例任务、两条模拟操作和导入示例留在本机，方便用户打开即查看；无真实联系人被操作。

## 仍受阻／未验证

- 评论分页与所选讨论回复均覆盖可识别文字；未选择的讨论、纯图片等未识别内容不会自动补齐。平台总数可能包含回复、不可访问或去重合并的评论，不能宣称等于平台总数。旧任务保留历史快照，旧版可能混入的自动展示回复不作无依据的删除或重分类。
- “主评论已到底”已在本次真实视频核验，只代表该次页面可识别主评论区域已结束；未展开的讨论、纯图片等内容仍不自动补齐。无新增内容或超时不会标记全部成功。

1. 真实“露营”网页读取首次返回 `pending_layout`：未识别到可见视频链接。任务 ID `76a20100ec8b4beb9ded628d258ca91f`，未取得真实视频、评论或用户记录。尚未确认具体原因是登录要求还是页面结构，不把它标为成功。
2. 后续带诊断读取任务 `fa45f15c41904bd891fe8d02783f2b1a` 仍为 `pending_layout`，未取得真实数据。页面标题为“发现更多精彩视频 - 抖音搜索”，可见文字包含导航、搜索分类与页脚，没有视频结果文字，未检测到明确登录或验证提示。仅能确认页面部分加载，不能断言为登录问题或平台限制。普通 Playwright 截图返回 `TimeoutError`，页面文字已独立保存于本机诊断 JSON。已暂停重复搜索，保留独立浏览器等待本人核对。
3. 登录和验证码必须由用户本人完成。未读取或索要用户密码。
4. 真实关注、私信适配器未实现；必须另行核实权限、测试对象与消息授权。
5. 其他电脑上的首次自动安装、长时间运行和平台改版兼容性尚未验证。
6. 回复规则已在两个真实视频验证，主评论到底在一个视频验证；页面类名和按钮文案可能随改版变化。原始 DOM 标记夹具是人工测试页面；新增类名和“展开更多”结构回归来自现场观察，昵称、正文与 ID 均替换为虚构值。

用户现场反馈：搜索页后来显示了视频，此前曾出现图形验证码。此为用户观察，工具尚未读取到真实视频数据。现有文本规则没有识别该次图形验证码，验证识别不完整；遇到不明页面仍停止，不能据此声称没有平台验证。保留当前浏览器，等待用户打开测试视频、展开评论，再走已有的当前页面只读接口；不重新搜索或自动处理验证码。

此前任务 `c16c71f2682e45659c7fadc99834a406` 捕获真实页面截图，但原代码因地址仍为 `/search/…` 而提前退出。已参照上游 `media_platform/douyin/help.py` 的 `modal_id` 约定修复，并新增指定视频链接入口，修复已加载到运行服务。用户提供链接后确认旧正文标记不存在；现场 DOM 显示正文为 `.FduGc_lz > .Sh1Da424`，适配保存在 `learning/selectors.json`。昵称优先使用有文字的用户链接，避免选到空头像链接。

成功任务：`c457bc24b7da4a9090530c40b24ff46f`；重复核验任务：`ac8bbf0d67a54eb5a3f50385fd2d09d5`。两次均为 `success`，各 14 评论、14 用户。未自动滚动、展开回复或操作真实关注和消息。真实数据与诊断只保存在本机，不包含在交付包中。

按用户新要求完成搜索/评论分离。100 条真实搜索任务：`f3df7f090c864486aa090e8a0924798c`，2026-09-09 16:09:43 开始、16:10:19 成功。现场卡片节点的 `id="waterfall_item_数字"` 提供真实视频编号；普通滚动逐批加载，按首次出现顺序去重保存 `search_rank`，不会把数据库 ID 排序当成搜索排名。遇无新增或验证时保留已存部分结果。搜索结果数量不足不代表全平台没有更多视频。

真实界面点击验证：在上述 100 条列表点击第 1 条视频 `7673121423547845915`，产生任务 `41daacac8944497dbf6974c778049ac0`，16:13:28 成功，4 评论 / 4 用户。HTTP 结果与界面“本视频：4 条评论 · 4 位用户”一致，评论旁显示昵称和主页链接。搜索原任务仍保留 100 条视频、0 条评论，评论独立保存。测试没有批量打开 100 个视频，也没有执行真实关注或私信。

## 重现测试

在项目目录使用已安装 `requirements-learning.txt` 的 Python：

```powershell
python -m pytest tests_learning -q
```

未设置 `LEARNING_UI_URL` 时 UI 测试会跳过。验证运行中的界面：

```powershell
$env:LEARNING_UI_URL='http://127.0.0.1:8765'
python -m pytest tests_learning/test_ui.py -q
```

在本地服务空闲运行于 8765、8876 空闲时，额外验证启动脚本（Windows）：

```powershell
$env:LEARNING_LAUNCHER_TEST='1'
python -m pytest tests_learning/test_launcher.py -q
```

同时设置两个变量后运行全部 `tests_learning`；当前共 62 项。不要对正在执行真实任务的实例运行启动器集成测试。

构建界面：在 `webui` 目录执行 `npm run build`。测试发现的启动误报根因是 Windows venv 启动器创建子进程，原脚本将启动器 PID 与服务实际 PID 比较；现在核验进程归属和父子关系，并保存服务实际 PID。启动器使用项目级互斥锁与已有服务记录，避免多端口重复实例；服务采用 Uvicorn 工厂入口，导入测试代码时不会初始化生产数据库。

上游工具链目前有 Browserslist 数据过期提示，测试依赖有一个 Starlette/AnyIO 弃用提示；均未导致本次检查失败。没有运行未使用的多平台采集器完整测试套件。

截图：`evidence/desktop.png`、`evidence/mobile.png`。


## 独立板块页面（2026-09-10）

- 七个独立页面：概览、搜索、任务、视频、用户、评论、日志。非当前板块不挂载，滚动不会进入其他板块。
- 哈希地址支持刷新、前进后退、旧书签兼容及任务详情恢复；各目录的筛选与列表页码在当前标签页会话内分别保存。
- 搜索创建后在搜索页显示本次进度，通过明确按钮进入结果；读取当前视频和按链接读取后进入视频详情。
- `npm run build` 成功；`tests_learning/test_library_ui.py tests_learning/test_ui.py`：8 passed，1 个既有 Starlette 弃用提示，33.08 秒。
- 覆盖七页内容隔离、直接打开地址、刷新、前进后退、目录筛选与第二页恢复、搜索进度入口、历史快照、用户抽屉返回、100 条评论分页、回复与导出。搜索提交使用隔离测试数据库及拦截接口，没有触发真实平台读取。
- 实际工作台已刷新至 `#keyword` 并核对：仅显示搜索表单，没有数据目录和日志。数据未迁移或删除，服务无需重启。
- `docs/evidence/independent-pages/` 为测试数据界面截图，已检查桌面任务页及 390px 无横向溢出断言；不作为真实数据读取成功证据。


## 搜索即时反馈与轻量转场（2026-09-10）

- 根因：按钮只有 POST 请求期间禁用；收到任务编号后恢复，运行进度卡在搜索表单与辅助入口下方，首屏不可见。
- 修复：提交请求时立即展示状态卡；确认后读取任务真实状态与视频数量。按钮持续显示运行状态并防重复提交；增加直接暂停、状态失联提示与核对入口。进度放在按钮下方，辅助入口折叠。
- 参考旧电商工作台 `src/styles.css` 的按钮按压、page-in、agent-run-progress 及 workspace-motion.css 的 .18/.22 秒节奏；未修改电商项目，也未套用 RunningHub 页面。
- 页面切换、详情、分组展开、侧栏打开增加短暂入场效果；统计卡和列表卡悬停浮起。系统减少动态效果时关闭动画。
- `npm run build` 成功（既有浏览器数据提示及单包约 504 KB 的体积提示）。
- `tests_learning/test_search_feedback_ui.py tests_learning/test_library_ui.py tests_learning/test_ui.py`：9 passed，1 个既有 Starlette 弃用提示，51.54 秒。
- 覆盖请求尚未返回时的即时反馈、运行中禁用重复提交、真实数量 37/100、刷新恢复、验证/部分/失败/暂停/完成状态、503 状态失联及恢复、暂停只提交一次、结果入口和减少动态效果；既有评论分页、抽屉、导出和七页隔离回归通过。
- 本轮使用拦截接口及隔离测试数据库验证交互，没有触发真实抖音搜索。截图 `docs/evidence/interaction-feedback/` 为合成测试记录，已检查 1366px 和 390px 布局，无横向溢出。
- 已刷新用户工作台；本地历史数据与后端读取逻辑保持原样。


## 本地记录删除（2026-09-10）

- 增加任务、视频、用户、评论、日志行删除，以及搜索进度卡中受阻/失败/已结束任务删除。
- 两步接口：先预览实际影响数量，再携带数据指纹确认；SQLite 单次事务提交。相关任务运行时拒绝删除，确认期间数据变化要求重新核对。
- 删除任务保留其他任务共用数据；视频、用户、评论按来源全局删除关联快照，主评论包含已存回复，清理不再引用的用户。重新生成共用实体的最新剩余快照。
- 删除旧批次分页状态，避免继续采集写回；任务删除墓碑拦截迟到结果和日志。网页与导入数据相互隔离。
- 界面包含取消/确认、影响数量、错误核对、重复提交锁、统计和筛选缓存刷新、空列表、当前搜索任务清理。删除日志只影响单条日志。
- 构建成功。全流程 34 passed / 1 warning / 66.05s；最终日志影响数量与无效筛选清理修正后，删除后端+删除界面+目录界面 15 passed / 1 warning / 23.82s。提示为既有 Starlette 弃用提醒。
- 测试使用临时数据库及接口拦截，覆盖六个删除入口、取消无写入、确认一次写入、刷新后不复现、共用数据保留、回复级联、来源隔离、运行中保护、数据变化保护和迟到写入。
- 实际服务已更新；只调用现有任务的删除预览，得到任务 1、日志 2、其余实体 0 的影响数量，未调用确认接口。核对时现有记录为任务 63、视频 718、用户 1220、评论 1281；用户自行操作可能继续改变这些数量。
- `docs/evidence/deletion/delete-confirmation.png` 为独立合成数据的弹窗截图，已视觉检查；不含真实平台删除操作。
- 本功能删除数据库中的本地记录；独立诊断截图/排错文件未清理，已在操作说明标明。删除不操作抖音平台。


## 批量选择与批量删除（2026-09-10）

- 累计任务提供当前页勾选/全选；视频在任务分组内选择；用户和评论在视频分组的当前 100 条内选择；日志支持当前显示记录的批量操作。
- 显示已选数量、取消选择、批量删除；切换分页/筛选/板块即清空选择，运行中任务及其日志不能勾选。
- 复用单条删除确认弹窗，增加所选名称列表与统一影响数量预览。提交目标冻结为已预览的集合，不逐条循环提交。
- 后端统一计算所选记录及关联数据的并集，在一个 SQLite 事务中提交。重复目标、任务共用实体、主评论与回复重叠均去重；任一目标缺失、相关任务运行或指纹变化时整批拒绝。
- 原单条接口复用同一删除引擎，保留来源隔离、共用数据保留、旧批次防写回及迟到日志保护。
- `npm run build` 成功（浏览器数据与约 514 KB 单包体积提示）。
- `test_batch_deletion.py test_batch_deletion_ui.py test_deletion.py test_deletion_ui.py test_library_ui.py`：23 passed，1 个既有 Starlette 弃用提示，55.76 秒。
- 测试涵盖五个批量入口、20 条任务页只选中 19 条非运行记录、跨页/筛选清空、取消无写入、确认只提交一次、刷新、390px 无横向溢出、重叠级联去重、整批拒绝及原单条删除和目录回归。
- `docs/evidence/batch-deletion/batch-confirmation.png` 为合成数据确认截图，已视觉检查。所有实际删除测试在临时数据库和拦截接口中进行，未删除用户现有记录。
- 已确认没有执行中任务后更新本地服务，并刷新工作台。


## 单对象关注与私信确认流程（2026-09-10）

- 增加独立账号操作页、打开登录浏览器、从用户详情带入目标、范围声明、身份/内容预览、5 分钟有效确认令牌、单次执行、操作历史与只读结果核对。
- 持久化账本按本人账号/对象/类型/消息去重。一次性令牌在执行前原子消费；执行中断或点击后无法核对则标记 uncertain，不允许重发。数据目录删除不清除此账本。
- 普通网页适配必须核对导航本人链接、目标主页 URL、唯一用户操作区；私信还需聊天窗口头部目标链接、聊天记录区、唯一输入/发送入口与本人消息标识。身份错误、其他草稿、验证、布局不匹配、不可发送时停止。
- 浏览器操作与采集共用单工作线程和 busy 锁，不实现批量发送、隐式执行、验证码绕过或私有签名接口。
- 最初本地 DOM 测试因测试 HTML 未声明 UTF-8 导致中文入口乱码而失败；为 fixture 加 charset 后通过，没有降低真实页面身份检查。
- 25 passed / 1 warning / 47.55 秒：账号账本、普通网页 fixture、账号页面、目录与既有工作流测试。最终登录入口与消息首尾空白规范化修正后，API 和账号界面 5 passed / 1 warning / 6.84 秒。提示为既有 Starlette 弃用提醒；前端构建成功（约 523 KB 单包体积提示）。
- 覆盖预览不点击关注/发送、明确确认后一次执行、发送者及接收者不匹配、验证、其他草稿、收到相同文本不误认为发出、发送无确认则 uncertain、刷新后不重发、只读核对、令牌过期、重复确认及重启恢复。
- 测试全部使用临时账本、拦截请求与本地构造网页，没有联系真实用户。真实账号账本核对为 0 条。
- `docs/evidence/account-actions/account-confirm.png` 为合成测试确认页，已查看桌面布局，测试含 390px 无横向溢出检查。
- **未实测/仍待接入验证：真实抖音 DOM 的兼容性、真实关注、真实私信。** 用户答复“待会登录”；本人主页、获准测试对象和内容尚未提供。严格选择器不能据此宣称已在抖音跑通，需登录后继续观察和适配。


## 2026-09-10 批量导出与视频下载

- 列表移除逐条删除图标，勾选后统一显示 CSV / JSON / 批量删除；视频另有批量下载。
- 复用现有 Playwright 下载事件与 save_as，未引入私有媒体接口或签名逻辑。官方依据：https://playwright.dev/python/docs/downloads 。
- SQLite 一致性快照导出，来源校验、去重、历史任务视频快照及 CSV 文本保护；下载请求编号去重、同一浏览器串行、逐条持久化状态、暂停与重启恢复、成功文件 ZIP。
- 本地样例验证：原生浏览器下载、视频文件头检查、没有下载入口、验证码停止后续视频、取消、重新加载任务状态。
- 相关回归首轮 28 项通过；截图 docs/evidence/batch-export/batch-actions.png 与 batch-actions-mobile.png 均为隔离测试数据。
- 真实抖音下载仍待验证，本轮没有向真实用户关注或发送消息，也没有执行真实数据删除或批量视频下载。

- 最终增量验证：18 项通过（导出、历史快照、导入来源日志、下载编号去重、文件下载/暂停、界面与原有工作流）；TypeScript/Vite 构建通过。


## 2026-09-10 视频分类搜索与停止原因

- 检查旧任务 7180d0c9a685408baf94d322d43ba2dd 与 bdfe2ee90f0940d89d90dea02793aba6 的本地诊断截图：均在综合分类，页尾明确显示“暂时没有更多了”。旧逻辑未单独识别结束标志，将连续 5 轮无新增统一报为部分结果。
- 新任务与继续读取先点击并确认视频分类。无新增等待改为 30 秒，本次读取上限 150 秒；等待时更新数量、等待秒数。结束提示须在卡片外、视口内连续两次出现，另保留登录/验证暂停。
- 新状态 search_exhausted（页面已到底）与 search_stalled（加载停滞），不把实际不足 100 条冒充 100 条成功，也不修改旧任务记录。
- 首次真实验证 0e98afcdefc2476dbebf3b6d138ab01e 已确认进入视频分类并保存 21 条；随后遇到动态列表节点替换导致滚动定位超时。依据页面已切至视频分类、任务日志与当时截图，改为一次读取实时 DOM 并滚动结果容器，消除分步定位已失效节点。
- 本地首轮 35 项通过（搜索、页面提示、现有读取流程）；滚动修正后搜索与浏览器 23 项通过。前端构建通过。验证码与取消均保留测试。

- 修正后的真实验证任务 43f2497981534232a1de7e447fd64553：关键词“广州 领养 猫”，已确认视频分类，共保存 64 条视频；随后连续 30 秒无新增，且无可见明确结束提示，状态为 search_stalled。诊断截图显示已滚动至最后一批结果，未再发生旧定位超时。只代表本次页面提供的结果；其中包含平台返回的弱相关内容，不保证全都符合领养主题，也不承诺每次都有 100 条。
- 真实诊断和数据保留在本机 learning_data，发行包不包含账号会话、数据库或真实用户诊断截图。

## 2026-09-10 实际账号与单对象操作适配

- 已从用户登录的工具浏览器读取可见抖音号。真实导航使用 `/user/self`，新增同会话临时页面核对，返回 `@抖音号`；没有复制其他浏览器的登录凭据。每次执行前重新检查账号。
- 真实用户资料区的关注/私信按钮位于资料节点的相邻区域。限定唯一资料节点与 `user-info-follow-btn` 所在父区，跳过推荐用户按钮；增加资料加载等待。
- 真实关注已点击一次，最初由于“相互关注”与原适配“互相关注”文字不同而记为不确定。刷新平台主页证实相互关注，补齐状态后通过只读 verify 更新为成功，没有再次点击关注。
- 私信依据实际 `#imSaasContainerId`、聊天标题打开的目标主页、Slate 编辑器和 `messageMessageBoxisFromMe` 标记适配。标题导航只等 URL 提交，不等待无关图片加载；已打开聊天面板时不再重复点击其下方私信入口。
- 实际编辑器在空白与文本末尾均保留 U+200B 光标占位。使用用户指定文本的临时草稿复现后清除，没有从该临时草稿发送消息。仅忽略编辑器边界占位，保留真实其他草稿；核对后仍需内容相符才可发送。
- 增加测试覆盖账号切换、延迟资料、相邻按钮与推荐区隔离、同名但错误聊天主页、收到同文消息、编辑器占位、图片一直加载及发送后刷新不持久化。所有本地测试使用隔离网页/账本，未向真实用户操作。
- 真实操作最终状态查看本地“关注与私信 → 操作记录”；私人账号、收件对象和聊天数据不打入发行包。
- 最终真实结果：关注与私信账本均为 success。关注仅点击一次；私信仅在最终文本核对通过后点击一次发送，消息为用户指定的“测试”，刷新后的会话显示本人同文消息。另在用户已登录的 Edge 同一目标聊天内只读核对到 1 条对应本人消息，没有在那里发送。此前页面/输入核对失败均发生在发送前。
- 最终回归：账号浏览器、账本/API、账号界面共 **19 passed，1 warning，52.36 秒**。界面测试使用 `@tester` 验证自动填入抖音号、预览确认和不确定状态刷新后不重发；截图仍为隔离测试数据。TypeScript/Vite 构建通过，产物 `index-Btr2bQOx.js`。警告为既有 Starlette 弃用提示；构建仍提示单包体积较大。
# 2026-09-10 浏览器同步画面与紧凑工作台

- 新增内存 JPEG 预览与同源 no-store 接口。仅采集本工具当前抖音页面；查看面板不创建浏览器、不导航、不执行搜索或账号操作。取图在原 Playwright 工作线程中执行，任务忙时由页面检查点更新，空闲时单次排队刷新；截图错误不使业务任务失败。
- 预览按需续期、限速，离开/收起后停止续期；当前画面延迟或连接失败均有明确提示。带放大对话框、Escape/焦点恢复和切到原浏览器按钮。
- 新增 5 个后端检查通过，包含真实浏览器渲染的红/蓝页面在任务线程仍繁忙时连续更新画面。测试拦截所有平台请求，仅使用临时目录，不采集真实用户数据。
- 新增界面测试验证并列布局、放大/关闭、旧画面提示、收起停止轮询、910px 并排和390px 无横向溢出。已查看 1440px、910px、390px 测试截图；图片明确标为本地测试页面。
- 组合回归首次 38 passed、1 failed；失败来自原账号 UI 测试替身尚未实现新预览接口。补齐替身后账号/预览 UI 2 passed，其他已通过的业务检查未改动。历史目录与搜索反馈 UI 检查亦通过。TypeScript/Vite 构建通过，仍有既有单包体积提示。
- 实际本机验证：打开工具浏览器，预览返回 live，真实抖音页面图像宽度 1280；面板图像加载成功，页面无横向溢出。没有发起新的采集、关注或私信。真实画面未保存到发行包。

## 2026-09-10 浏览器面板真实搜索验收

- 从工作台启动一次“东莞二手车”真实搜索。点击后立即显示“正在读取”、禁用重复启动、展示收集进度和暂停入口；最终状态 success，保存 100 条不同视频。
- 点击“查看本次搜索结果”进入该任务的视频页，实际渲染 100 个视频入口。910px 窗口中结果与浏览器画面并排，图片加载成功，无横向溢出。
- 预览序号从搜索前 315 更新到搜索后 361，后续为 483；后续 live 帧距采样约 1.3 秒。此次未单独采样任务忙时的接口状态；忙时更新由前述隔离浏览器测试覆盖。
- 与上次同关键词的 100 条结果重合 96 条，全局视频为并集 104 条，没有因重复搜索增加重复视频；原有 3 条评论和 3 个用户仍保留。
- 平台结果含少量弱相关内容；本次达到 100 条不代表后续每次均能达到，也不保证每条语义相关。未执行关注、私信、删除或视频下载。真实数据与预览图像不包含在发行包中。

## 2026-09-10 数据列表单列与长文省略

- 任务详情和目录分组内的视频均固定单列，序号和标题横向排列；批量选择框置于行首。标题单行省略，悬停可看全文，点击仍进入完整视频详情。
- 任务名、视频分组名和用户名增加省略处理。评论、回复、用户抽屉中的评论及日志正文最多显示两行，实际溢出时出现可用键盘操作的“展开全文／收起全文”，不截断保存或导出的内容。
- TypeScript/Vite 构建通过。已有历史记录与独立页面流程回归 2 passed（含筛选、评论分页、用户抽屉、导出、返回、移动端宽度）；仍有既有 Starlette 弃用和包体积提示。
- 实际工作台检查已有“广州 租房”任务：100 个视频入口单列，任务详情行高约 42px，目录行高约 63px，长标题省略且无横向溢出。只查看已保存数据，没有启动采集或账号操作。
# 2026-09-10 按需进入批量选择

- 统一批量选择组件新增显式选择模式，默认仅显示“选择”；退出时清空勾选并隐藏行首与全选框。筛选或翻页改变选择范围后自动退出，运行中记录仍禁选。视频行不再保留隐藏勾选框的空白占位。
- TypeScript/Vite 构建通过。批量删除、分组选择、导出与下载反馈、单条删除确认流程共 12 passed（43.05 秒），全部使用隔离数据库和本地替身；包含默认隐藏、取消后重新进入清零以及分页/筛选重置。
- 实际工作台任务页默认 0 个勾选框；点击选择后显示 4 条记录框与 1 个全选框；取消后隐藏。没有删除真实记录或启动真实下载。
## 2026-09-10 顶栏新手指南

- 顶栏全局入口顺序为“新手指南 → 连接状态 → 主题”。点击用 Radix 对话框打开，不切换路由或执行任务；默认展开登录章节，配两个窗口的用途示意，另含首次启动、搜索验证、评论用户、批量管理、账号操作与常见问题。
- 已在实际工作台检查入口位置、弹窗显示、Escape 关闭与触发按钮焦点恢复；无页面横向溢出。Markdown 版同步放入 docs/新手使用指南.md，并在学习版 README 链接。
- TypeScript/Vite 构建通过，已有目录/页面回归 2 passed（13.37 秒），包含移动端宽度检查。没有新建真实任务、登录、关注或发送消息。
